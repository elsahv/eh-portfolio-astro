---
import { Image } from 'astro:assets';

export interface Props {
    title: string;
    p: string;
    image: any;
}

const { title, p, image } = Astro.props as Props
---


<div class="my-5 h-screen">
    <a
    class="hover:opacity-80 hover:ease-in duration-300"
    href="/"
  >
     <!-- <h3>{title}</h3> -->
     <div class="pb-1 pl-1">
        <!-- <span class="text-aquamarine text-2xl">{p}</span> -->
        <Image
        class="2xl:w-[1020px] w-full drop-shadow-3xl border-2 border-onyx"
        src={image}
        alt={title}
      />
    </a>
     </div>
    </div> 