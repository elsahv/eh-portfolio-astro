---
const frontmatter = Astro.props;
const { title, image, alt, url, testItem1 } = frontmatter;
---

<div id="sq" class="py-5 opacity-80 hover:opacity-100">
  <div class="flex font-semibold pt-1 pb-1 pl- pr-1 bg-">
    <h3 class="text-slate-600 text-xl">...{title}</h3>
  </div>

  <a href={url}>
    <img
      src={image}
      class="border border-black drop-shadow-xl border border-gray-300"
      alt={alt}
    />
  </a>

  <div class="pl- pt- flex">
    <p class="mr-2 px-2 pb-1 bg-">
      <!-- - {testItem1} -->
      this is a description of sites, clients, and audience served
    </p>
  </div>
</div>
